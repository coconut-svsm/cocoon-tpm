# Cocoon TPM project - `cocoon-tpm-utils-async` crate

This `[no_std]` crates bundles Rust `async` related utilities used by
other crates in the project. See the output of `cargo doc` for a
reference.
